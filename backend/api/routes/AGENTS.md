# Route Modules

## Purpose
- Put route handlers here once the API is scaffolded.
- Keep handlers thin: validation, service call, response mapping.

## Rule
- Business logic belongs in `backend/analytics` or adjacent service modules, not inline in route files.

